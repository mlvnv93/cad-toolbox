"use client";

import Link from "next/link";
import { useEffect, useMemo, useRef, useState } from "react";
import ThemeToggle from "@/components/ThemeToggle";
import DrawingPreviewLayer from "./DrawingPreviewLayer";
import { createDrawingPreviewRequest, type DrawingPreview } from "./draw-preview";
import { createDrawingExportRequest, type DrawingExportFormat } from "./draw-export";

type ViewName = "Front" | "Back" | "Left" | "Right" | "Top" | "Bottom" | "Isometric";
type DimensionType = "Linear" | "Angular" | "Diameter" | "Radius";
type Position = { x: number; y: number };
type PreviewView = { entities: DrawingPreview["entities"]; bounds: { min_x: number; min_y: number; max_x: number; max_y: number }; center: Position; position: Position };
type BackendPreview = { units: string; sheet: { width_mm: number; height_mm: number; paper_size: string }; scale: { label: string; factor: number }; projection_type: string; views: Record<string, PreviewView> };

const viewNames: ViewName[] = ["Front", "Back", "Left", "Right", "Top", "Bottom", "Isometric"];
const viewSymbols: Record<ViewName, string> = { Front: "F", Back: "B", Left: "L", Right: "R", Top: "T", Bottom: "D", Isometric: "I" };
const paperDimensions: Record<string, [number, number]> = { A4: [210, 297], A3: [297, 420], A2: [420, 594], A1: [594, 841] };

function PanelSection({ title, children, badge }: { title: string; children: React.ReactNode; badge?: string }) { return <details className="cad3d-draw-section" open><summary><span>{title}</span>{badge && <em>{badge}</em>}<span className="cad3d-section-chevron" aria-hidden="true">⌄</span></summary>{children}</details>; }
function SettingRow({ label, children }: { label: string; children: React.ReactNode }) { return <div className="cad3d-setting"><span className="cad3d-setting-label">{label}</span><div className="cad3d-setting-control">{children}</div></div>; }
function sheetDimensions(size: string, orientation: "portrait" | "landscape"): [number, number] { const [width, height] = paperDimensions[size] ?? paperDimensions.A4; return orientation === "portrait" ? [width, height] : [height, width]; }
function defaultPositions(width: number, height: number): Record<ViewName, Position> { return { Front: { x: width * .32, y: height * .4 }, Back: { x: width * .32, y: height * .4 }, Left: { x: width * .32, y: height * .4 }, Right: { x: width * .68, y: height * .4 }, Top: { x: width * .32, y: height * .68 }, Bottom: { x: width * .32, y: height * .16 }, Isometric: { x: width * .5, y: height * .4 } }; }

export default function DrawWorkbench() {
  const [leftOpen, setLeftOpen] = useState(true), [rightOpen, setRightOpen] = useState(true), [file, setFile] = useState<File | null>(null);
  const [preview, setPreview] = useState<BackendPreview | null>(null), [previewLoading, setPreviewLoading] = useState(false), [previewError, setPreviewError] = useState(false);
  const [paperSize, setPaperSize] = useState("A4"), [customWidth, setCustomWidth] = useState(210), [customHeight, setCustomHeight] = useState(297), [orientation, setOrientation] = useState<"portrait" | "landscape">("portrait"), [projectionType, setProjectionType] = useState("THIRD_ANGLE"), [scale, setScale] = useState("automatic");
  const [placedViews, setPlacedViews] = useState<ViewName[]>(["Front", "Top", "Right"]), [positions, setPositions] = useState<Record<ViewName, Position>>(defaultPositions(210, 297));
  const [selectedView, setSelectedView] = useState<ViewName | null>("Front"), [draggedView, setDraggedView] = useState<ViewName | null>(null), [movingView, setMovingView] = useState<ViewName | null>(null);
  const [showDimensions, setShowDimensions] = useState(true), [showAnnotations, setShowAnnotations] = useState(true), [dimensionType, setDimensionType] = useState<DimensionType | null>(null), [dimensionPoints, setDimensionPoints] = useState<number[]>([]);
  const [titleBlockOpen, setTitleBlockOpen] = useState(false), [title, setTitle] = useState("800000182_1"), [partNumber, setPartNumber] = useState("PART-001");
  const [exportFormat, setExportFormat] = useState<DrawingExportFormat>("pdf"), [exporting, setExporting] = useState(false), [gridVisible, setGridVisible] = useState(false), [gridSpacing, setGridSpacing] = useState("10"), [customGridSpacing, setCustomGridSpacing] = useState(10), [fullscreen, setFullscreen] = useState(false);
  const workbenchRef = useRef<HTMLDivElement>(null);
  const [fallbackWidth, fallbackHeight] = useMemo(() => paperSize === "Custom" ? [customWidth, customHeight] as [number, number] : sheetDimensions(paperSize, orientation), [paperSize, orientation, customWidth, customHeight]);
  const sheetWidth = preview?.sheet.width_mm ?? fallbackWidth, sheetHeight = preview?.sheet.height_mm ?? fallbackHeight;
  const scaleLabel = preview?.scale.label ?? "calculating...", gridSize = gridSpacing === "custom" ? customGridSpacing : Number(gridSpacing);
  const border = Math.min(5, Math.max(2, sheetWidth * .02)), titleBlockHeight = 32, titleBlockWidth = Math.min(70, sheetWidth * .34), titleBlockX = sheetWidth - titleBlockWidth - border, titleBlockY = sheetHeight - titleBlockHeight - border;
  const layerPreview = useMemo<DrawingPreview | null>(() => {
    if (!preview) return null;
    const views = Object.fromEntries(viewNames.map((name) => { const key = name.toLowerCase(); return [key, preview.views[key]?.entities ?? []]; })) as DrawingPreview["views"];
    const view_positions = Object.fromEntries(viewNames.map((name) => { const key = name.toLowerCase(); return [key, preview.views[key]?.position ?? positions[name]]; })) as DrawingPreview["view_positions"];
    return { units: preview.units, sheet_width_mm: preview.sheet.width_mm, sheet_height_mm: preview.sheet.height_mm, scale: preview.scale.factor, scale_label: preview.scale.label, layers: [], entities: [], views, view_positions };
  }, [preview, positions]);

  useEffect(() => { const onFullscreenChange = () => setFullscreen(document.fullscreenElement === workbenchRef.current); const onEscape = (event: KeyboardEvent) => { if (event.key === "Escape" && !document.fullscreenElement) setFullscreen(false); }; document.addEventListener("fullscreenchange", onFullscreenChange); document.addEventListener("keydown", onEscape); return () => { document.removeEventListener("fullscreenchange", onFullscreenChange); document.removeEventListener("keydown", onEscape); }; }, []);
  const toggleFullscreen = async () => { if (document.fullscreenElement === workbenchRef.current) await document.exitFullscreen(); else if (workbenchRef.current?.requestFullscreen) await workbenchRef.current.requestFullscreen(); else setFullscreen((value) => !value); };
  const loadPreview = async (nextFile: File) => { setPreviewLoading(true); setPreviewError(false); setPreview(null); try { const request = createDrawingPreviewRequest(process.env.NEXT_PUBLIC_DRAWING_API_URL, nextFile, paperSize, orientation, projectionType); request.body.append("scale", scale); if (paperSize === "Custom") { request.body.append("custom_width_mm", String(customWidth)); request.body.append("custom_height_mm", String(customHeight)); } const response = await fetch(request.url.replace("/draw/preview", "/drawings/preview"), { method: "POST", body: request.body }); if (!response.ok) throw new Error("Preview generation failed"); const nextPreview = await response.json() as BackendPreview; setPreview(nextPreview); setPositions((current) => Object.fromEntries(Object.entries(current).map(([name, position]) => [name, nextPreview.views[name.toLowerCase()]?.position ?? position])) as Record<ViewName, Position>); } catch { setPreview(null); setPreviewError(true); } finally { setPreviewLoading(false); } };
  useEffect(() => { if (!file) return; const timer = window.setTimeout(() => { void loadPreview(file); }, 150); return () => window.clearTimeout(timer); }, [file, paperSize, orientation, projectionType, scale, customWidth, customHeight]);
  const addView = (view: ViewName) => { setPlacedViews((current) => current.includes(view) ? current : [...current, view]); setSelectedView(view); };
  const handleFileChange = (event: React.ChangeEvent<HTMLInputElement>) => { const nextFile = event.target.files?.[0]; if (nextFile) { setFile(nextFile); setPreview(null); setPreviewError(false); setTitle(nextFile.name.replace(/\.(step|stp)$/i, "")); setPositions(defaultPositions(fallbackWidth, fallbackHeight)); } };
  const beginDimension = (type: DimensionType) => { setDimensionType(type); setDimensionPoints([]); };
  const handleSheetClick = (event: React.MouseEvent<SVGSVGElement>) => { if (!dimensionType) return; const bounds = event.currentTarget.getBoundingClientRect(); setDimensionPoints((current) => current.length >= 4 ? current : [...current, ((event.clientX - bounds.left) / bounds.width) * sheetWidth, ((event.clientY - bounds.top) / bounds.height) * sheetHeight]); };
  const moveView = (event: React.PointerEvent<SVGSVGElement>) => { if (!movingView) return; const bounds = event.currentTarget.getBoundingClientRect(); setPositions((current) => ({ ...current, [movingView]: { x: ((event.clientX - bounds.left) / bounds.width) * sheetWidth, y: ((event.clientY - bounds.top) / bounds.height) * sheetHeight } })); };
  const exportDrawing = async () => { if (!file || exporting) return; setExporting(true); try { const request = createDrawingExportRequest(process.env.NEXT_PUBLIC_DRAWING_API_URL, exportFormat, file, paperSize, orientation, projectionType, scale, customWidth, customHeight); const response = await fetch(request.url, { method: "POST", body: request.body }); if (!response.ok) throw new Error("Drawing export failed"); const url = URL.createObjectURL(await response.blob()); const link = document.createElement("a"); link.href = url; link.download = `${title || "drawing"}.${request.filenameExtension}`; link.click(); URL.revokeObjectURL(url); } catch { window.alert("We could not export the drawing. Load a STEP file and try again."); } finally { setExporting(false); } };
  const loadStatus = !file ? "No CAD model loaded" : previewError ? "Model load failed" : previewLoading ? "Loading STEP model..." : preview ? "STEP model loaded" : "Preparing model...";

  return <main className="cad3d-draw-page"><header className="cad3d-draw-header"><Link className="cad3d-draw-brand" href="/"><span className="cad3d-brand-mark" aria-hidden="true" />CAD3D KIT</Link><div className="cad3d-draw-nav"><Link href="/">← Back to Home</Link><ThemeToggle /></div></header>
    <section className="cad3d-draw-intro"><div><span className="cad3d-draw-kicker">DRAW · TECHNICAL WORKBENCH</span><h1>Drawing</h1><p>Create a technical drawing from your CAD model.</p></div><div className="cad3d-draw-privacy"><span className="cad3d-status-dot" />Your files stay private · processed in your browser</div></section>
    <div className="cad3d-draw-filebar"><label className="cad3d-primary-button">{file ? "Replace CAD file" : "Load CAD file"}<input type="file" accept=".step,.stp" onChange={handleFileChange} /></label><div className="cad3d-file-summary"><strong>{file?.name || "No CAD file loaded"}</strong><span>{file ? `${(file.size / 1024 / 1024).toFixed(2)} MB · STEP` : "STEP / STP"}</span></div><div className={`cad3d-file-state ${preview ? "ready" : previewError ? "error" : ""}`}><span className="cad3d-status-dot" />{loadStatus}</div></div>
    <div ref={workbenchRef} className={`cad3d-draw-workbench ${fullscreen ? "is-fullscreen" : ""}`}>
      <aside className={`cad3d-draw-panel ${leftOpen ? "" : "collapsed"}`}><button className="cad3d-panel-collapse" type="button" onClick={() => setLeftOpen((value) => !value)} aria-label="Toggle drawing settings">{leftOpen ? "‹" : "›"}</button>{leftOpen && <div className="cad3d-panel-content"><div className="cad3d-panel-title"><span>1 · SETTINGS</span><h2>Drawing setup</h2></div><PanelSection title="File information"><SettingRow label="File"><strong>{file?.name || "Not loaded"}</strong></SettingRow><div className={`cad3d-inline-status ${preview ? "ready" : ""}`}>{loadStatus}</div></PanelSection><PanelSection title="Sheet"><SettingRow label="Paper size"><select value={paperSize} onChange={(event) => setPaperSize(event.target.value)}><option>A4</option><option>A3</option><option>A2</option><option>A1</option><option>Custom</option></select></SettingRow>{paperSize === "Custom" && <><SettingRow label="Width (mm)"><input type="number" min="10" max="5000" value={customWidth} onChange={(event) => setCustomWidth(Number(event.target.value))} /></SettingRow><SettingRow label="Height (mm)"><input type="number" min="10" max="5000" value={customHeight} onChange={(event) => setCustomHeight(Number(event.target.value))} /></SettingRow></>}<SettingRow label="Orientation"><span className="cad3d-segmented"><button type="button" className={orientation === "portrait" ? "active" : ""} onClick={() => setOrientation("portrait")}>Portrait</button><button type="button" className={orientation === "landscape" ? "active" : ""} onClick={() => setOrientation("landscape")}>Landscape</button></span></SettingRow><SettingRow label="Scale"><select value={scale} onChange={(event) => setScale(event.target.value)}><option value="automatic">Automatic</option><option value="1:1">1:1</option><option value="1:2">1:2</option><option value="1:3">1:3</option><option value="2:1">2:1</option><option value="1:10">1:10</option><option value="1:15">1:15</option></select></SettingRow><SettingRow label="Projection"><select value={projectionType} onChange={(event) => setProjectionType(event.target.value)}><option value="THIRD_ANGLE">Third angle</option><option value="FIRST_ANGLE">First angle</option></select></SettingRow></PanelSection><PanelSection title="Grid"><SettingRow label="Show grid"><input type="checkbox" checked={gridVisible} onChange={(event) => setGridVisible(event.target.checked)} /></SettingRow><SettingRow label="Spacing"><select value={gridSpacing} onChange={(event) => setGridSpacing(event.target.value)}><option value="1">1 mm</option><option value="2">2 mm</option><option value="5">5 mm</option><option value="10">10 mm</option><option value="20">20 mm</option><option value="25">25 mm</option><option value="50">50 mm</option><option value="custom">Custom</option></select></SettingRow>{gridSpacing === "custom" && <SettingRow label="Custom mm"><input type="number" min="0.1" value={customGridSpacing} onChange={(event) => setCustomGridSpacing(Number(event.target.value))} /></SettingRow>}</PanelSection></div>}</aside>
      <section className="cad3d-draw-canvas" aria-label="Technical drawing sheet"><div className="cad3d-canvas-toolbar"><div><strong>Technical drawing</strong><span>{paperSize} · {orientation === "portrait" ? "Portrait" : "Landscape"} · {scaleLabel}</span></div><div className="cad3d-canvas-actions"><span className="cad3d-canvas-state">{previewLoading ? "Generating views…" : previewError ? "Preview unavailable" : preview ? "Model ready" : "Waiting for CAD file"}</span><button type="button" onClick={toggleFullscreen}>{fullscreen ? "Exit full screen" : "Full screen"}</button></div></div><div className="cad3d-sheet-viewport"><svg className="cad3d-drawing-sheet" viewBox={`0 0 ${sheetWidth} ${sheetHeight}`} preserveAspectRatio="xMidYMid meet" onClick={handleSheetClick} onPointerMove={moveView} onPointerUp={() => setMovingView(null)} onDragOver={(event) => event.preventDefault()} onDrop={(event) => { event.preventDefault(); if (draggedView) addView(draggedView); }}><defs><pattern id="drawing-grid" width={gridSize} height={gridSize} patternUnits="userSpaceOnUse"><path d={`M ${gridSize} 0 L 0 0 0 ${gridSize}`} className="cad3d-grid-line" fill="none" /></pattern></defs><rect className="cad3d-sheet-paper" width={sheetWidth} height={sheetHeight} />{gridVisible && <rect width={sheetWidth} height={sheetHeight} fill="url(#drawing-grid)" aria-hidden="true" />}<rect className="cad3d-sheet-border" x={border} y={border} width={sheetWidth - border * 2} height={sheetHeight - border * 2} />{placedViews.map((view) => <DrawingPreviewLayer key={view} preview={layerPreview} view={view} position={positions[view]} selected={selectedView === view} onSelect={() => setSelectedView(view)} onPointerDown={() => setMovingView(view)} />)}{previewLoading && <text className="cad3d-sheet-message" x={sheetWidth / 2} y={sheetHeight - 10} textAnchor="middle">Generating model views…</text>}{previewError && <text className="cad3d-sheet-message" x={sheetWidth / 2} y={sheetHeight - 10} textAnchor="middle">Model preview unavailable</text>}{dimensionType && <text className="cad3d-sheet-message" x={sheetWidth / 2} y={sheetHeight - 10} textAnchor="middle">{dimensionType} dimension · {dimensionPoints.length ? "Select the next point" : "Select a point on the sheet"}</text>}{showAnnotations && <g className="cad3d-title-block"><line x1={titleBlockX} y1={titleBlockY} x2={titleBlockX + titleBlockWidth} y2={titleBlockY} /><line x1={titleBlockX} y1={titleBlockY + titleBlockHeight} x2={titleBlockX + titleBlockWidth} y2={titleBlockY + titleBlockHeight} /><line x1={titleBlockX} y1={titleBlockY} x2={titleBlockX} y2={titleBlockY + titleBlockHeight} /><line x1={titleBlockX + titleBlockWidth} y1={titleBlockY} x2={titleBlockX + titleBlockWidth} y2={titleBlockY + titleBlockHeight} /><text x={titleBlockX + 4} y={titleBlockY + 7}>TITLE BLOCK</text><text x={titleBlockX + 4} y={titleBlockY + 15}>{title || "UNTITLED DRAWING"}</text><text x={titleBlockX + 4} y={titleBlockY + 23}>{partNumber} · REV A</text></g>}</svg></div><p className="cad3d-canvas-hint">{dimensionType ? `${dimensionType} Dimension — Select ${dimensionType === "Linear" ? "two points, edges or vertices" : "a feature on the drawing"}` : "Drag a view from the right panel onto the sheet · Select and reposition placed views"}</p></section>
      <aside className={`cad3d-draw-panel cad3d-right-panel ${rightOpen ? "" : "collapsed"}`}><button className="cad3d-panel-collapse" type="button" onClick={() => setRightOpen((value) => !value)} aria-label="Toggle drawing tools">{rightOpen ? "›" : "‹"}</button>{rightOpen && <div className="cad3d-panel-content"><div className="cad3d-panel-title"><span>2 · TOOLS</span><h2>Drawing tools</h2></div><PanelSection title="Views" badge={`${viewNames.filter((view) => Boolean(preview?.views[view.toLowerCase()])).length}/7`}><p className="cad3d-panel-help">Drag a view to the sheet</p><div className="cad3d-view-list">{viewNames.map((view) => { const available = Boolean(preview?.views[view.toLowerCase()]); const placed = placedViews.includes(view); return <button className={`cad3d-view-row ${selectedView === view ? "selected" : ""} ${placed ? "placed" : ""}`} disabled={!available} draggable={available} onDragStart={() => setDraggedView(view)} onClick={() => addView(view)} key={view} type="button"><span className="cad3d-drag-grip">⋮⋮</span><span className="cad3d-view-symbol">{viewSymbols[view]}</span><span className="cad3d-view-name">{view}</span><span className="cad3d-view-status">{available ? placed ? "Placed" : "Ready" : "Waiting"}</span></button>; })}</div></PanelSection><PanelSection title="Dimensions"><SettingRow label="Show dimensions"><input type="checkbox" checked={showDimensions} onChange={(event) => setShowDimensions(event.target.checked)} /></SettingRow><div className="cad3d-tool-grid">{(["Linear", "Angular", "Diameter", "Radius"] as DimensionType[]).map((type) => <button key={type} type="button" className={dimensionType === type ? "active" : ""} onClick={() => beginDimension(type)}>{type}</button>)}</div>{dimensionType && <p className="cad3d-context-help">{dimensionType} Dimension — select {dimensionType === "Linear" ? "two points, edges or vertices" : "a feature on the drawing"}.</p>}</PanelSection><PanelSection title="Annotations"><SettingRow label="Show annotations"><input type="checkbox" checked={showAnnotations} onChange={(event) => setShowAnnotations(event.target.checked)} /></SettingRow><div className="cad3d-check-list"><label><input type="checkbox" defaultChecked /> Centre marks</label><label><input type="checkbox" defaultChecked /> Centre lines</label><label><input type="checkbox" /> Notes</label></div></PanelSection><PanelSection title="Title block"><button className="cad3d-outline-button" type="button" onClick={() => setTitleBlockOpen(true)}>Edit title block</button></PanelSection></div>}</aside>
      <nav className="cad3d-draw-toolbar" aria-label="Drawing tools"><ToolbarButton icon="⌁" label="Select" active /><ToolbarButton icon="✥" label="Pan" /><ToolbarButton icon="＋" label="Zoom" /><ToolbarButton icon="□" label="Fit" /><ToolbarButton icon="DIM" label="Dimensions" active={showDimensions} onClick={() => setShowDimensions((value) => !value)} /><ToolbarButton icon="ANN" label="Annotations" active={showAnnotations} onClick={() => setShowAnnotations((value) => !value)} /><ToolbarButton icon="▣" label={fullscreen ? "Exit full screen" : "Full screen"} active={fullscreen} onClick={toggleFullscreen} /><select aria-label="Export format" value={exportFormat} onChange={(event) => setExportFormat(event.target.value as DrawingExportFormat)} disabled={exporting}><option value="pdf">PDF</option><option value="dxf">DXF</option></select><button className="cad3d-export" type="button" onClick={exportDrawing} disabled={exporting}>{exporting ? "Exporting…" : `Export ${exportFormat.toUpperCase()}`}</button></nav>
    </div>
    {titleBlockOpen && <div className="cad3d-modal-backdrop"><form className="cad3d-modal" onSubmit={(event) => { event.preventDefault(); setTitleBlockOpen(false); }}><div className="cad3d-modal-kicker">TITLE BLOCK</div><h2>Edit title block</h2><label>Drawing title<input value={title} onChange={(event) => setTitle(event.target.value)} /></label><label>Part number<input value={partNumber} onChange={(event) => setPartNumber(event.target.value)} /></label><div className="cad3d-modal-actions"><button type="button" onClick={() => setTitleBlockOpen(false)}>Cancel</button><button className="cad3d-primary-button" type="submit">Apply changes</button></div></form></div>}
    <style jsx global>{`
      .cad3d-draw-page{min-height:100vh;background:#f7f3eb;color:#17243a;padding-bottom:28px}.cad3d-draw-header{height:64px;display:flex;align-items:center;justify-content:space-between;padding:0 32px;border-bottom:1px solid #d9dfe5;background:#fffdf8}.cad3d-draw-brand{display:flex;align-items:center;gap:10px;font-size:17px;font-weight:760;letter-spacing:-.04em}.cad3d-brand-mark{width:22px;height:22px;border:1.5px solid #087f7a;border-radius:3px;display:block;position:relative}.cad3d-brand-mark:after{content:"";position:absolute;left:5px;top:5px;width:9px;height:9px;border:1.5px solid #2468c9;transform:rotate(30deg)}.cad3d-draw-nav{display:flex;align-items:center;gap:20px;color:#5f6d7e;font-size:12px}.cad3d-draw-nav a:hover{color:#087f7a}.cad3d-draw-intro{width:min(1440px,calc(100% - 48px));margin:0 auto;padding:24px 4px 16px;display:flex;align-items:end;justify-content:space-between;gap:24px}.cad3d-draw-kicker{font-size:10px;letter-spacing:.12em;color:#087f7a;font-weight:750}.cad3d-draw-intro h1{font-size:30px;line-height:1;margin:7px 0 6px;letter-spacing:-.05em}.cad3d-draw-intro p{margin:0;color:#5f6d7e;font-size:13px}.cad3d-draw-privacy{display:flex;align-items:center;gap:8px;color:#68778a;font-size:11px;padding-bottom:4px}.cad3d-status-dot{width:7px;height:7px;border-radius:50%;background:#13a6a1;display:inline-block;flex:none}.cad3d-draw-filebar{width:min(1440px,calc(100% - 48px));margin:0 auto 12px;min-height:56px;border:1px solid #d9dfe5;background:#fffdf8;display:flex;align-items:center;gap:16px;padding:10px 14px}.cad3d-primary-button{display:inline-flex;align-items:center;justify-content:center;gap:8px;background:#087f7a;color:white;border:0;border-radius:4px;padding:9px 13px;font-size:11px;font-weight:700;cursor:pointer}.cad3d-primary-button input{position:absolute;width:1px;height:1px;opacity:0;pointer-events:none}.cad3d-file-summary{display:flex;flex-direction:column;gap:3px;min-width:220px}.cad3d-file-summary strong{font-size:12px}.cad3d-file-summary span{font-size:10px;color:#758397}.cad3d-file-state{margin-left:auto;display:flex;align-items:center;gap:8px;font-size:11px;color:#758397}.cad3d-file-state.ready{color:#087f7a}.cad3d-file-state.error{color:#b64d59}.cad3d-draw-workbench{width:min(1440px,calc(100% - 48px));margin:0 auto;min-height:calc(100vh - 180px);display:grid;grid-template-columns:245px minmax(420px,1fr) 245px;grid-template-rows:minmax(560px,1fr) 56px;gap:1px;background:#cfd6df;border:1px solid #cfd6df;overflow:hidden}.cad3d-draw-workbench.is-fullscreen{width:100vw;height:100vh;min-height:100vh;margin:0;border:0}.cad3d-draw-panel{position:relative;grid-row:1/2;background:#fffdf8;min-width:0}.cad3d-panel-content{height:100%;overflow:auto;padding:18px 16px 24px}.cad3d-panel-title{padding:2px 4px 15px;border-bottom:1px solid #e3e7ec;margin-bottom:4px}.cad3d-panel-title span{font-size:9px;letter-spacing:.1em;font-weight:750;color:#087f7a}.cad3d-panel-title h2{font-size:17px;letter-spacing:-.035em;margin:5px 0 0}.cad3d-panel-collapse{position:absolute;z-index:4;top:13px;right:-12px;width:24px;height:24px;border:1px solid #cfd6df;background:#fffdf8;border-radius:50%;font-size:18px;line-height:20px;color:#526174;cursor:pointer}.cad3d-draw-panel.collapsed{width:0;min-width:0}.cad3d-draw-panel.collapsed .cad3d-panel-collapse{right:-12px}.cad3d-draw-panel.collapsed .cad3d-panel-content{display:none}.cad3d-draw-section{border-bottom:1px solid #e3e7ec;padding:0 0 3px}.cad3d-draw-section summary{list-style:none;cursor:pointer;display:flex;align-items:center;gap:7px;padding:13px 3px 10px;font-size:11px;font-weight:700}.cad3d-draw-section summary::-webkit-details-marker{display:none}.cad3d-draw-section summary em{margin-left:auto;color:#087f7a;font-size:9px;font-style:normal}.cad3d-section-chevron{margin-left:auto;color:#8996a7;font-size:14px}.cad3d-draw-section[open] .cad3d-section-chevron{transform:rotate(180deg)}.cad3d-setting{display:grid;grid-template-columns:82px minmax(0,1fr);gap:9px;align-items:center;margin:7px 3px}.cad3d-setting-label{font-size:10px;color:#68778a}.cad3d-setting-control{min-width:0}.cad3d-setting select,.cad3d-setting input[type=number]{width:100%;height:31px;border:1px solid #ccd4df;background:#fff;color:#17243a;border-radius:3px;padding:0 8px;font-size:10px;outline:none}.cad3d-setting select:focus,.cad3d-setting input:focus{border-color:#087f7a;box-shadow:0 0 0 2px rgba(8,127,122,.1)}.cad3d-segmented{display:grid;grid-template-columns:1fr 1fr;border:1px solid #ccd4df;border-radius:3px;overflow:hidden}.cad3d-segmented button{height:29px;border:0;background:#fff;color:#68778a;font-size:9px;cursor:pointer}.cad3d-segmented button+button{border-left:1px solid #ccd4df}.cad3d-segmented button.active{background:#e7f4f2;color:#087f7a;font-weight:700}.cad3d-inline-status{margin:9px 3px 12px;padding:8px 9px;border:1px solid #e1e6ec;background:#f6f7f9;color:#758397;font-size:9px}.cad3d-inline-status.ready{border-color:#b9dfd9;background:#eff9f7;color:#087f7a}.cad3d-canvas{background:#e9edf2;min-width:0;display:flex;flex-direction:column;position:relative}.cad3d-canvas-toolbar{min-height:52px;background:#fffdf8;border-bottom:1px solid #d9dfe5;display:flex;align-items:center;justify-content:space-between;padding:0 15px;gap:12px}.cad3d-canvas-toolbar>div:first-child{display:flex;flex-direction:column;gap:3px}.cad3d-canvas-toolbar strong{font-size:12px}.cad3d-canvas-toolbar span{font-size:9px;color:#758397}.cad3d-canvas-actions{display:flex;align-items:center;gap:10px}.cad3d-canvas-actions button{height:28px;border:1px solid #ccd4df;background:#fff;border-radius:3px;padding:0 9px;font-size:9px;color:#526174;cursor:pointer}.cad3d-canvas-state{padding:5px 8px;border:1px solid #dfe4ea;background:#f8f9fa;border-radius:3px}.cad3d-sheet-viewport{flex:1;min-height:0;display:flex;align-items:center;justify-content:center;overflow:auto;padding:28px;background:#dfe4e9}.cad3d-drawing-sheet{width:min(94%,850px);height:auto;max-height:calc(100% - 8px);filter:drop-shadow(0 8px 18px rgba(23,36,58,.14));background:white;cursor:default}.cad3d-sheet-paper{fill:#fffdf8}.cad3d-sheet-border{fill:none;stroke:#26384f;stroke-width:.7}.cad3d-grid-line{stroke:#cbd5df;stroke-width:.25}.cad3d-sheet-message{fill:#68778a;font-size:4px}.cad3d-canvas-hint{text-align:center;margin:0;padding:8px 12px;background:#fffdf8;border-top:1px solid #d9dfe5;color:#68778a;font-size:9px}.cad3d-title-block{fill:none;stroke:#26384f;stroke-width:.45}.cad3d-title-block text{fill:#26384f;font-size:3.1px;font-family:Arial,sans-serif}.cad3d-right-panel{grid-column:3}.cad3d-panel-help{margin:0 3px 9px;color:#7b899a;font-size:9px}.cad3d-view-list{display:flex;flex-direction:column;gap:3px}.cad3d-view-row{width:100%;display:grid;grid-template-columns:14px 25px 1fr auto;align-items:center;gap:6px;border:1px solid transparent;background:transparent;border-radius:3px;padding:7px 5px;text-align:left;color:#34445a;cursor:pointer}.cad3d-view-row:hover:not(:disabled),.cad3d-view-row.selected{background:#edf7f5;border-color:#c5e4df}.cad3d-view-row:disabled{opacity:.42;cursor:not-allowed}.cad3d-drag-grip{color:#a1adbb;font-size:11px}.cad3d-view-symbol{width:23px;height:23px;display:grid;place-items:center;border:1px solid #bfcbd7;color:#087f7a;font-size:9px;font-weight:750;background:#fff}.cad3d-view-name{font-size:10px}.cad3d-view-status{font-size:8px;color:#8491a2}.cad3d-view-row.placed .cad3d-view-status{color:#087f7a}.cad3d-tool-grid{display:grid;grid-template-columns:1fr 1fr;gap:5px;margin:9px 3px}.cad3d-tool-grid button{height:29px;border:1px solid #ccd4df;background:#fff;border-radius:3px;font-size:9px;color:#526174;cursor:pointer}.cad3d-tool-grid button.active{background:#e7f4f2;border-color:#9bcfc7;color:#087f7a;font-weight:700}.cad3d-context-help{margin:8px 3px;color:#68778a;font-size:9px;line-height:1.45}.cad3d-check-list{display:flex;flex-direction:column;gap:8px;padding:5px 3px 10px}.cad3d-check-list label{font-size:9px;color:#526174}.cad3d-check-list input{margin-right:7px}.cad3d-outline-button{width:100%;height:31px;border:1px solid #b9c5d2;background:#fff;border-radius:3px;color:#087f7a;font-size:10px;font-weight:700;cursor:pointer;margin:3px 0 9px}.cad3d-outline-button:hover{background:#eff9f7}.cad3d-draw-toolbar{grid-column:1/4;display:flex;align-items:center;gap:5px;padding:7px 10px;background:#fffdf8;border-top:1px solid #cfd6df}.cad3d-toolbar-button{height:40px;min-width:54px;border:1px solid #d2d9e1;background:#fff;border-radius:3px;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:2px;color:#526174;cursor:pointer}.cad3d-toolbar-button span{font-size:12px;font-weight:700;line-height:1}.cad3d-toolbar-button small{font-size:7px}.cad3d-toolbar-button.active{border-color:#9bcfc7;background:#eff9f7;color:#087f7a}.cad3d-draw-toolbar select{margin-left:auto;height:32px;border:1px solid #ccd4df;background:#fff;border-radius:3px;padding:0 7px;font-size:9px}.cad3d-export{height:32px;border:0;background:#087f7a;color:white;border-radius:3px;padding:0 12px;font-size:9px;font-weight:750;cursor:pointer}.cad3d-export:disabled{opacity:.55;cursor:wait}.cad3d-modal-backdrop{position:fixed;inset:0;z-index:50;display:grid;place-items:center;background:rgba(23,36,58,.34)}.cad3d-modal{width:min(420px,calc(100% - 32px));padding:24px;background:#fffdf8;border:1px solid #d9dfe5;box-shadow:0 20px 50px rgba(23,36,58,.2)}.cad3d-modal-kicker{font-size:9px;letter-spacing:.1em;color:#087f7a;font-weight:750}.cad3d-modal h2{font-size:20px;margin:6px 0 18px}.cad3d-modal label{display:flex;flex-direction:column;gap:6px;margin:12px 0;font-size:10px;color:#68778a}.cad3d-modal input{height:34px;border:1px solid #ccd4df;padding:0 9px;font-size:11px}.cad3d-modal-actions{display:flex;justify-content:flex-end;gap:7px;margin-top:18px}.cad3d-modal-actions button{height:32px;padding:0 12px;border:1px solid #ccd4df;background:#fff;color:#526174;border-radius:3px;font-size:10px;cursor:pointer}.cad3d-modal-actions .cad3d-primary-button{border:0;background:#087f7a;color:#fff}.cad3d-draw-workbench.is-fullscreen .cad3d-sheet-viewport{padding:38px}.cad3d-draw-workbench.is-fullscreen .cad3d-drawing-sheet{width:min(92%,1200px)}
      @media(max-width:1100px){.cad3d-draw-workbench{grid-template-columns:220px minmax(360px,1fr) 220px}.cad3d-file-summary{min-width:160px}}
      @media(max-width:820px){.cad3d-draw-intro,.cad3d-draw-filebar{width:calc(100% - 24px)}.cad3d-draw-intro{align-items:flex-start;flex-direction:column}.cad3d-draw-workbench{width:calc(100% - 24px);grid-template-columns:1fr;grid-template-rows:auto minmax(520px,1fr) auto 56px}.cad3d-draw-panel,.cad3d-right-panel{grid-column:1;grid-row:auto}.cad3d-canvas{grid-column:1;grid-row:2;min-height:520px}.cad3d-right-panel{grid-row:3}.cad3d-draw-toolbar{grid-column:1;grid-row:4;overflow-x:auto}.cad3d-filebar{flex-wrap:wrap}.cad3d-file-state{margin-left:0}.cad3d-draw-header{padding:0 16px}.cad3d-draw-nav>a{display:none}}
    `}</style>
  </main>;
}

function ToolbarButton({ icon, label, active, onClick }: { icon: string; label: string; active?: boolean; onClick?: () => void }) { return <button className={`cad3d-toolbar-button ${active ? "active" : ""}`} type="button" onClick={onClick} title={label}><span aria-hidden="true">{icon}</span><small>{label}</small></button>; }